{
  "name": "socket-server",
  "version": "0.0.1",
  "description": "Socket server to track location",
  "scripts": {
    "dev": "./node_modules/.bin/nodemon",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.5.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
